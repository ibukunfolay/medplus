import React from "react";

const Notes = () => {
  return (
    <div className="notes">
      <div className="note">Note</div>
      <div className="note">Note</div>
      <div className="note">Note</div>
      <div className="note">Note</div>
    </div>
  );
};

export default Notes;
